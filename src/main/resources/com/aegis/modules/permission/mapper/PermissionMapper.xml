<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aegis.modules.permission.mapper.PermissionMapper">

    <resultMap id="BaseResultMap" type="com.aegis.modules.permission.domain.entity.Permission">
        <id property="id" column="id"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
        <result property="version" column="version"/>
        <result property="permCode" column="perm_code"/>
        <result property="permName" column="perm_name"/>
        <result property="permType" column="perm_type"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,create_by,update_by,create_time,update_time,deleted,
        version,perm_code,perm_name,perm_type,status,
        remark
    </sql>

    <select id="selectPermCodesByUserId" resultType="java.lang.String">
        SELECT DISTINCT rp.perm_code
        FROM t_role_permission rp
                 INNER JOIN t_user_role ur ON ur.role_id = rp.role_id
                 INNER JOIN t_role r ON r.id = rp.role_id
                 INNER JOIN t_permission p ON p.perm_code = rp.perm_code
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND r.status = '0'
          AND p.deleted = 0
          AND p.status = '0'
    </select>
</mapper>
