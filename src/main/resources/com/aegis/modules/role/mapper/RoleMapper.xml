<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aegis.modules.role.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.aegis.modules.role.domain.entity.Role">
        <id property="id" column="id"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
        <result property="version" column="version"/>
        <result property="remark" column="remark"/>
        <result property="roleName" column="role_name"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="menuCheckStrictly" column="menu_check_strictly"/>
        <result property="deptCheckStrictly" column="dept_check_strictly"/>
        <result property="status" column="status"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,create_by,update_by,create_time,update_time,deleted,
        version,remark,role_name,role_code,role_sort,
        data_scope,menu_check_strictly,dept_check_strictly,status
    </sql>

    <select id="getCustomDeptIds" resultType="java.lang.Long">
        SELECT DISTINCT rd.dept_id
        FROM t_role_dept rd
                 INNER JOIN t_role r ON rd.role_id = r.id
                 INNER JOIN t_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.data_scope = '2'
          AND r.deleted = 0
    </select>

    <select id="getHighestDataScope" resultType="java.lang.String">
        SELECT MIN(r.data_scope)
        FROM t_role r
                 INNER JOIN t_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND r.status = '0'
    </select>
</mapper>
